import React from 'react';

const TodoFilter = () => {
  const style = {
    width: '100%',
    height: '50px',
    backgroundColor: 'lightgray'
  }
  return (
    <div style={style}>
      filter by : completed
    </div>
  )
};

export default TodoFilter;
